const buildAnchorTraitDefinition = (placeholderText) => ({
  type: 'text',
  name: 'id',
  label: 'Anchor name (for menu links)',
  placeholder: placeholderText,
});

export default buildAnchorTraitDefinition;
